import React, { useState } from "react";
import Button from "Components/Button";
import Switch from "Components/Switch";
import Stack from "../src/helpers/story-stack";
import Example from "../src/helpers/story-example";
import Wrapper from "./genericWrapper";

export const DefaultButton = () => {
  const [disabled, toggleDisabled] = useState(0);
  const [loading, toggleLoading] = useState(0);
  return (
    <React.Fragment>
      <Example title="Large">
        <Stack>
          <Button size="large" disabled={disabled} loading={loading}>
            Button
          </Button>
          <Button
            appearance="outline"
            size="large"
            btnColor="red"
            disabled={disabled}
            loading={loading}
          >
            Outline
          </Button>
          <Button
            btnColor="red"
            size="large"
            disabled={disabled}
            loading={loading}
          >
            Red
          </Button>
        </Stack>
      </Example>
      <Example title="Default Button">
        <Stack align="space-between">
          <Button disabled={disabled} loading={loading}>
            Button
          </Button>
          <Button appearance="outline" disabled={disabled} loading={loading}>
            Outline
          </Button>
          <Button btnColor="red" disabled={disabled} loading={loading}>
            Red
          </Button>
          <Button
            appearance="outline"
            btnColor="red"
            disabled={disabled}
            loading={loading}
          >
            Outline
          </Button>
        </Stack>
      </Example>

      <Example title="Default Button">
        <Stack align="space-between">
          <Button disabled={disabled} loading={loading} btnColor="brown">
            Button
          </Button>
          <Button
            appearance="outline"
            disabled={disabled}
            loading={loading}
            btnColor="blue"
          >
            blue
          </Button>
          <Button disabled={disabled} loading={loading} btnColor="orange">
            orange
          </Button>
          <Button
            appearance="outline"
            disabled={disabled}
            loading={loading}
            btnColor="orange"
          >
            orange
          </Button>
          <Button
            appearance="outline"
            disabled={disabled}
            loading={loading}
            btnColor="teal"
          >
            teal
          </Button>
        </Stack>
      </Example>
      <Example>
        <Stack align="left">
          <Wrapper>
            {" "}
            <h2>DisAble : </h2>
          </Wrapper>
          <Wrapper>
            <Switch onToggle={flag => toggleDisabled(flag)} active={disabled} />
          </Wrapper>
        </Stack>
        <Stack align="left">
          <Wrapper>
            <h2>isLoading : </h2>
          </Wrapper>
          <Wrapper>
            <Switch
              onToggle={flag => toggleLoading(flag)}
              active={loading}
              readOnly={disabled}
            />
          </Wrapper>
        </Stack>
      </Example>
    </React.Fragment>
  );
};
