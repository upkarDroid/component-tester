import React from "react";
import { storiesOf } from "@storybook/react";
import ImageUploader from "Components/ImageUploader";
import Stack from "../src/helpers/story-stack";
import Example from "../src/helpers/story-example";

storiesOf("Image Uploader", module).addWithJSX("Default", () => (
  <div>
    <Example title="Image Uploader">
      <Stack>
        <ImageUploader
          label="Add Photos"
          secondSourceLink="www.google.com"
          secondSourceLbl="Add from Instagram"
          btnLabel="Add Photos"
          btnSubTitle="or drop files to upload"
          onChange={x => ({
            success: false,
            errorMsg: "File type must be .jpg, .png or .gif"
          })}
        />
      </Stack>
    </Example>
  </div>
));
